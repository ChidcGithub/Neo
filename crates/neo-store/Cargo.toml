[package]
name = "neo-store"
description = "Neo 的本地持久化：SQLite 会话 / 消息 / 设置"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
rusqlite = { workspace = true }
